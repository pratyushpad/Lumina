# Base URL of the Lumina API.
# Local dev (docker compose / make dev-backend):
VITE_API_URL=http://localhost:8000
# Production (Vercel env var) points at the deployed API:
#   VITE_API_URL=https://prat20-lumina-api.hf.space
